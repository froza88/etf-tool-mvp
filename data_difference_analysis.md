# 最稳定版本 vs 当前版本 - 数据区别分析

## 版本信息
- **最稳定版本**: 2026051804 (2971238, 12:34)
- **当前版本**: 2026051820 (76332b8, 18:51)

---

## 核心区别对比

| 对比维度 | 最稳定版本 (12:34) | 当前版本 (18:51) | 影响 |
|---------|-------------------|-----------------|------|
| **实时价格** | ✅ 1466/1466 (100%) | ✅ 1466/1466 (100%) | 无区别 |
| **涨跌幅 (change_pct)** | ✅ 修复，准确 | ✅ 修复，准确 | 无区别 |
| **昨收 (prev_close)** | ✅ 准确 | ✅ 准确 | 无区别 |
| **历史K线覆盖** | ⚠️ **前500只** (34%) | 🔴 **0/1466** (0%) | ⚠️ 当前版失败 |
| **风险指标覆盖** | ⚠️ **前500只** (34%) | 🔴 **0/1466** (0%) | ⚠️ 当前版失败 |
| **数据更新方式** | 全量更新 | 增量更新（有缓存机制） | 当前版更优（但未生效）|
| **API限流处理** | 0.15秒间隔，无重试 | 0.3秒间隔 + 重试机制（代码已写但未测试）| 当前版理论更优 |
| **列访问方式** | iloc[2], iloc[3] | **列名访问**（防错位）| 当前版更健壮 |

---

## 数据覆盖详细对比

### 1. 实时数据（价格、涨跌幅）
```
最稳定版本: ✅ 1466/1466 = 100%
当前版本:   ✅ 1466/1466 = 100%
结论: ✅ 无区别
```

### 2. 历史K线（用于计算风险指标）
```
最稳定版本: ✅ 500/1466 = 34% (有500限制)
当前版本:   🔴 0/1466 = 0% (API限流失败)
结论: ⚠️ 稳定版更好（至少500只有数据）
```

### 3. 风险指标（夏普、回撤、年化收益）
```
最稳定版本: ✅ 500/1466 = 34% (有500限制)
当前版本:   🔴 0/1466 = 0% (依赖历史K线)
结论: ⚠️ 稳定版更好
```

---

## 问题根因分析

### 当前版本为什么失败？
```python
# 当前版代码（已添加重试机制）
df = fetch_etf_history_with_retry(ak, code, start_date, end_date)
#            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#            这个函数会重试3次，但第一次就失败了
#            原因：AKShare API 连接被拒绝
```

**错误日志：**
```
[18:59:53] Step 2: 拉取历史K线并计算风险指标 (全部 1466 只)...
[18:59:53]   加载已有缓存: 5 只
[19:00:09]   进度: 50/1466 成功=0 失败=50 跳过=0  ← 全部失败！
```

**根本原因：**
- AKShare 调用东方财富 API
- 1466 只 ETF × 0.15秒间隔 = 太快
- 触发反爬机制：`Connection aborted, Remote end closed connection`

---

## 立即行动方案

### 方案 A：回滚到稳定版 + 保留好的改动 **[推荐]**
```bash
cd /Users/apangduo/WorkBuddy/Claw/etf-tool-mvp

# 1. 恢复稳定版的 daily_update.py
git checkout 2971238 -- daily_update.py

# 2. 应用当前版的改进（列名访问）
# （手动合并代码，只改 update_prices 函数）

# 3. 测试
python3 daily_update.py

# 4. 提交
git add daily_update.py
git commit -m "fix: 回滚到稳定版 + 应用列名访问改进"
```

**结果：**
- ✅ 500 只 ETF 有历史数据
- ✅ 价格数据准确（列名访问）
- ✅ 不会 API 限流

---

### 方案 B：修复当前版（加重试 + 降速）
```python
# 已添加的代码（需要测试）
def fetch_etf_history_with_retry(ak, code, start_date, end_date, max_retries=3):
    for attempt in range(max_retries):
        try:
            df = ak.fund_etf_hist_em(...)
            return df
        except:
            wait = 2 ** attempt  # 1s, 2s, 4s
            time.sleep(wait)
    return None

# 还需要加大间隔
time.sleep(0.5)  # 改成 0.5秒 或 1秒
```

**结果：**
- ✅ 1466 只 ETF 都会有数据（理论上）
- ⚠️ 需要 1466 × 0.5秒 = 733秒（12分钟）
- ⚠️ 可能还是会被限流

---

### 方案 C：混合方案（最佳实践）
```python
# 1. 保留500限制（保证速度）
# 2. 加重试机制（提高成功率）
# 3. 增量更新（避免重复下载）

sorted_etfs = sorted(etfs, key=lambda x: x.get('scale', 0) or 0, reverse=True)
target_etfs = sorted_etfs[:500]  # 只处理前500只

# 但对这500只，用重试机制
for etf in target_etfs:
    df = fetch_etf_history_with_retry(ak, code, start, end)
    # ...
    time.sleep(0.5)  # 加大间隔
```

**结果：**
- ✅ 500 只 ETF 有完整数据
- ✅ 不会被限流
- ✅ 执行时间可控（500 × 0.5秒 = 250秒）

---

## 建议

**立即执行方案 A：**
```bash
cd /Users/apangduo/WorkBuddy/Claw/etf-tool-mvp
git checkout 2971238 -- daily_update.py
python3 daily_update.py  # 验证能跑
```

**原因：**
1. 稳定版至少有 500 只 ETF 的数据（34% 覆盖）
2. 当前版是 0 覆盖（全部失败）
3. 先恢复服务，再慢慢优化

---

## 数据质量对比图

```
实时价格:
稳定版 ████████████████████ 100%
当前版 ████████████████████ 100%
结论: ✅ 一样好

历史K线:
稳定版 ██████░░░░░░░░░░░░░░  34% (500只)
当前版 ░░░░░░░░░░░░░░░░░░░░   0% (全部失败)
结论: ⚠️ 稳定版更好

风险指标:
稳定版 ██████░░░░░░░░░░░░░░  34% (500只)
当前版 ░░░░░░░░░░░░░░░░░░░░   0% (全部失败)
结论: ⚠️ 稳定版更好
```

---

## 总结

**当前版虽然代码更先进（增量更新、重试机制、列名访问），但实际效果是 0% 数据覆盖（因为 API 限流）。**

**稳定版虽然有限制（500只），但实际效果是 34% 数据覆盖（可正常运行）。**

**建议：先回滚到稳定版，让系统跑起来，再慢慢修复当前版的限流问题。**
