# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
*.egg-info/
dist/
build/

# Flask
instance/
.webassets-cache

# 数据库
*.db
*.sqlite

# 日志
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# 系统
.DS_Store
Thumbs.db

# 临时文件
*.bak
*.backup
*.tmp

# 虚拟环境
.env
.venv
env/
venv/

# SSH 密钥（误提交风险）
# 一路回车*
*.pub
id_*

# 临时数据文件（重建产物，不应提交）
etf_codes_matched.json
etf_complete_130_cleaned.json
etf_complete_130_corrected.json
etf_complete_130_final.json
etf_complete_130_from_feitu.json
etf_data_rebuilt.json

# 旧脚本（已被新版本替代）
fix_names.py
refetch_holdings.py

# enrich 日志（后台运行产生的）
enrich_*.log
rebuild*.log

# 数据目录（大文件，由 pipeline 生成，不应提交到 git）
# data/history/ 下的文件太大，不提交
data/history/*.json
# data/backup/ 下的备份文件，不提交
data/backup/
# data/snapshots/ 下的快照文件，不提交（太大）
data/snapshots/
# data/realtime/ 缓存文件，不提交
data/realtime/
# meta.json 可以提交（很小，记录版本信息）
